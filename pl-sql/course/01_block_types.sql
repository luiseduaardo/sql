/* nome (caso não seja anônimo) */
DECLARE
    -- declaration statement (opcional)
        -- identifiers, variables, cursors, etc.
BEGIN
    -- executable statements (obrigatório)
        -- 
EXCEPTION 
    -- exception handling statements (opcional)
END; /* nome (caso não seja anônimo) */
/